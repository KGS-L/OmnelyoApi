---
scope: Whole platform: api/ SaaS core, workers/ durable queue, bot/ Telegram+OAuth, core/ video pipeline, migrations/
purpose: Ratify brownfield conventions into a build-substrate spine plus one human-facing artifact
altitude: System (initiative) -> features/epics
updated: 2026-08-16T03:45
---

- (event) Code sweep completed: 3 parallel explorers (api/, workers/, bot+core+migrations+deploy) + README/decisions/implementation docs read
- (constraint) PostgreSQL is the sole business persistence; SQLite stack removed 2026-08-16, only __pycache__ husks remain in db/ scheduler/ billing/
- (constraint) Redis stores only transient state (OTP, OAuth state, telegram link tokens, rate-limit, wakeup); rate-limit and wakeup degrade (fail-open / PG polling) but OTP/OAuth/link flows hard-depend on Redis
- (constraint) Prices come exclusively from provider_price_mappings, never from the client; webhooks verified (Dodo signatures, MoneyFusion server-side get_payment confirmation)
- (constraint) Every schema change via dedicated Alembic migration; linear chain 20260813_0001..0017, URL injected from API_DATABASE_URL via migrations/env.py
- (constraint) Naming decided 2026-08-16: shortpilot internal (code/env/CI), omnelyo public (domains, image, VPS paths)
- (constraint) R2 keys are workspace-scoped (workspaces/{ws}/videos|jobs/...), buckets private, reads via presigned URLs only; storage_keys.belongs_to_workspace re-verified server-side
- (decision) Deployment shape: one image, five compose services (postgres, redis, migrate one-shot, api uvicorn, worker python -m workers.main, bot python main.py); Binds process boundaries; Prevents per-service image drift
- (decision) Tenancy: explicit workspace_id filters in every query behind get_current_workspace_membership dependency; foreign workspace = 404 not 403; no tenant middleware, no repository layer; Binds data access; Prevents cross-tenant leaks
- (decision) Job queue: PostgreSQL-owned durable queue, claim via FOR UPDATE SKIP LOCKED, leases + heartbeat thread + stale recovery; Redis wakeup is latency optimization only; Binds async work; Prevents dual sources of job truth
- (decision) Credits: append-only ledger, balance = SUM(entries), reserve/capture/release lifecycle tied to RENDER job outcome; idempotency always a unique constraint on (scope, idempotency_key)
- (decision) Integrations: SocialPublisher ABC (connect/exchange/list_channels/validate_media/publish/get_status/cancel/refresh_credentials) with registry; adapters storage-free; LLM via OpenAI-compatible provider registry; TTS OpenAI-only
- (question) Config fork: root config.py (os.getenv + import-time mkdir, serves bot/core/worker-handlers) vs api/config.py pydantic-settings; latent prod crash: mkdir on read_only fs in prod overlay; dead JOB_* knobs disagree with model defaults. Unify?
- (question) Pipeline chaining: no handler enqueues the next stage; web client drives INGEST->PROCESS->RENDER->PUBLISH via POST /jobs. Intentional or accident? Keep client-driven or move to worker-orchestrated?
- (question) Redis-optional invariant scope: login, social connect and telegram link hard-require Redis today. Declare Redis required-for-auth in the spine, or build PG fallbacks?
- (question) Orphaned bot YouTube OAuth: Flask :8420 server boots in every deployment but /connect_youtube handlers never registered; core/youtube_uploader.py imported nowhere; file-based tokens vs Fernet-encrypted DB tokens. Retire the legacy path?
- (question) Dead-letter policy: FAILED after max_attempts is permanent (retry endpoint refuses), cancel is QUEUED-only so long RENDER/PUBLISH cannot be stopped. Ratify or fix?
- (question) User-facing language: API errors French, bot messages mix French/English, no i18n convention. Ratify French-only user-facing?
- (question) PUBLISH double-execution window: lease checked before publish call but no ownership check after; no provider idempotency key; _persist_result overwrites external_id unconditionally. Zombie worker can double-publish
- (question) Human artifact purpose/audience: not yet drawn out (coaching round 1 pending)
- (direction) User requests all communication in French; human-facing artifact should therefore be French (aligns with AGENTS.md doc convention; overrides BMAD document_output_language=English) — confirm with user at artifact step
- (event) User delegated fork decisions to the architect (coaching round 1 unanswered); Fast-path assumptions applied per recommendation
- (decision) AD: single config system = api/config.py pydantic-settings with production validators; root config.py to be retired (latent read_only prod crash, dead JOB_* knobs, duplicated YouTube default); migration via bmad-build
- (decision) AD: pipeline chaining stays client-driven (API surface is the orchestrator, schema forces video_id per stage); deferred: worker auto-chain if frontend proves burden
- (decision) AD: Redis dependency scoped honestly: required for auth/link flows (OTP, OAuth state, Telegram link), best-effort degrading for rate-limit (fail-open) and worker wakeup (PG polling)
- (decision) AD: single social-connector invariant: every platform connection lives in SocialConnection with Fernet-encrypted tokens; legacy bot OAuth path (Flask :8420, core/youtube_auth.py, core/youtube_uploader.py) to retire
- (decision) AD: dead-letter semantics: FAILED is terminal and permanent after max_attempts; cancel only from QUEUED; recovery = new job or manual DB edit
- (decision) AD: user-facing language is French everywhere (API errors, bot messages); docs French per AGENTS.md
- (decision) DEBT (revisit condition): PUBLISH double-execution window (no provider idempotency key, unconditional external_id overwrite) — revisit before multi-worker scale-out or first duplicate incident
- (event) User asked: which BMAD agent for improving/fixing social integrations using Postiz open-source code as reference (path recorded: ~/Bureau/Open-Source-Outils-IA-Agents/postiz-app/libraries/nestjs-libraries/src/integrations); answer: bmad-build with the path as reference input
- (event) Reviewer gate: lint 2 low (verified false positives: {workspace_id}/{ws} are intentional URI/key patterns); 3 parallel reviewers (rubric, versions, adversarial) all pass-with-findings; full reviews in reviews/
- (event) Gate fixes applied: AD-4 tightened (all job transitions exclusively via workers/job_state.py under row lock), AD-6 tightened (at most one non-terminal job per (video_id,type) and per publication), AD-7 tightened (WorkspaceEntitlement single writer = fulfilment service under row lock); Stack + yt-dlp/dodopayments/apiMoneyFusion pins + Python CI skew note; topology corrections (six services, bot legacy volumes, read_only api/worker only, scripts/ vs deploy/); 9 new Deferred items (scheduled-dispatch owner, enqueue dedup impl, media_assets accounting, GDPR purge vs financial records, channel sharing, audit coverage, tenant-filter guard, AI/email settings migration, Redis 8 window)
- (event) spine finalized (status: final); human artifact APERCU-ARCHITECTURE.html built in French; deliverables: ARCHITECTURE-SPINE.md + APERCU-ARCHITECTURE.html + .memlog.md (32 entries) + reviews/ (3)
